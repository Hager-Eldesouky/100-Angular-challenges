import { ImageLazyloadingDirective } from './image-lazyloading.directive';

describe('ImageLazyloadingDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLazyloadingDirective();
    expect(directive).toBeTruthy();
  });
});
